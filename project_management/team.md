# Team Organisation

> Replace the placeholders with your real team information.

## Members

| Member | Login | Main responsibilities |
|--------|-------|-----------------------|
| `<name 1>` | `<login1>` | `<e.g. maze adapter, config, packaging>` |
| `<name 2>` | `<login2>` | `<e.g. rendering, UI, ghost AI>` |

## How we worked

- **Decisions:** `<how decisions were made — e.g. discussed in person, agreed
  on the architecture in the README before coding>`.
- **Task split:** `<who owned which modules>`. The modular architecture (one
  concern per module) let us work in parallel with few conflicts.
- **Communication:** `<channel — e.g. Discord + pair sessions>`.
- **Code review:** `<how you reviewed each other's code before merging>`.

## Issue handling

- `<how blocking points were escalated and resolved — see retro.md>`.
